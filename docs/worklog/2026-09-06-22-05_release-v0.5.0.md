## Agent signature
- **Agent**: `claude-fable-5-1`
- **Working tree**: `/home/edkjo/workspace/inbox/ifcfast`
- **Branch**: `main` @ `3baeb30` → `12ef9e1` (1 commit: the release)
- **Session scope**: cut and publish v0.5.0; regenerate ifcfast.com receipts against the shipped wheel
- **Touched paths**: CHANGELOG.md, Cargo.toml, Cargo.lock, pyproject.toml, scripts/generate_receipts.py (stamp one-liner), docs/worklog/
- **Parallel sessions observed**: none
- **Supersedes / superseded by**: none

## Summary

v0.5.0 is on PyPI and GitHub. Ed's call on the number was delegated
("full confidence"); 0.5.0 was chosen because the Layer-1 correctness
programme is complete (every named QTO/mesh residue shipped or parked)
and the bundle carries two cache-schema bumps (28 → 30), first-class
federation, and the clash-oracle harness. Semver-wise a 0.x minor.

- `CHANGELOG.md`: Unreleased → `[0.5.0] - 2026-09-06` with a lead
  paragraph; added the sections the changelog had never recorded
  (#143 narrow-phase perf incl. the intersection-first commit, #142
  STEP escapes, #141 clash oracle + mesh round-trip oracle, receipts
  generator). Link ref added.
- Versions bumped in `Cargo.toml`, `pyproject.toml`, `Cargo.lock`
  (`cargo metadata --offline` validated the lockfile).
- Tag `v0.5.0` pushed → release.yml run 34070150528: linux x64,
  windows, macos arm64 + intel, sdist, publish — all green. ~35 min
  wall-clock (macos-intel is the long pole).
- GitHub release created with notes:
  https://github.com/EdvardGK/ifcfast/releases/tag/v0.5.0
- Receipts regenerated with the **PyPI wheel** (fresh venv
  `scratch/venv-0.5.0`, py3.13, ifcopenshell 0.8.5), not the dev .so:
  all counts identical to the 0.4.42-dev run, GLBs byte-identical,
  only measured timings moved (~5–10 % slower — a Next.js server and a
  headless Chromium were running alongside). Written straight into
  `../ifcfast-site/public/receipts/`.

## Site (ifcfast-site, branch `receipts` @ `258c78f` → `7ea244c`)

See `ifcfast-site/docs/worklog/2026-09-06-22-05_receipts-0.5.0-build-fix.md`.
Short version: the branch had never built after the Tailwind fix — a
generator/page contract drift (`oracle.rounds` vs `oracle.pair_recall`)
crashed prerender. Fixed on the page side, verified at 390×844 and
1440×900, pushed. **Merge to master is blocked for the agent by the
permission classifier** — Ed runs it.

## Evidence

- `pip index versions ifcfast` lagged ~2 min behind the publish;
  `pip install ifcfast==0.5.0` succeeded on the first retry loop.
- `receipts` PyPI badge is a `next: { revalidate: 600 }` fetch; a local
  rebuild served the cached 0.4.42 — production revalidates itself.

## Next

1. **Ed**: merge `receipts` → `master` in ifcfast-site (production
   auto-deploys), then check https://ifcfast.com on the phone.
2. Post-0.5.0 backlog, in order: #170 adaptive circle tessellation
   (Plumbing 1.9 M tris / 12 MB GLB; oracle-gate), #168 RIV fan/coil,
   #166 `MeshStats.by_source`, #146 engine-side GUID materials.
3. `scratch/g55/cache_*` + `scratch/venv-0.5.0` are several GB — ask
   Ed before trashing.
