## Agent signature
- **Agent**: `claude-fable-5-1` (coordinator; opus/sonnet agents did the edits)
- **Working tree**: `/home/edkjo/workspace/inbox/ifcfast`
- **Branch**: `main` @ `45f4562` → `f3c2982` (one squashed fix commit; this worklog follows)
- **Session scope**: fix the five tester-filed issues #177–#180 + #183 (and the #181/#184 follow-ups they forced), oracle-gated, and ship
- **Touched paths**: `crates/core/src/{indexer.rs,lib.rs,mesh/brep.rs,mesh/faceset.rs}`, `crates/wasm/src/{analysis.rs,lib.rs}`, `crates/wasm/test/parity.mjs`, `python/ifcfast/{__init__.py,cache.py,cli.py,federated_floors.py,header.py,model.py,whitelist.py}`, `scripts/{generate_receipts.py,generate_sample_sidecars.py}`, `AGENTS.md` (+ packaged copy), 5 fixtures + 5 test files under `tests/`; site repo `ifcfast-site` `12fdfc3` (sidecars + wasm sync)
- **Parallel sessions observed**: `none` on origin/main during the window (14:00–16:00). Issues #178/#179/#180/#183 were filed today by the edkjo-side agent ("edkjo, ifc-check/skiplum" signature) before this session started.
- **Supersedes / superseded by**: `none`

## Summary

Five issues, four fix agents, one review agent, one gate run, one commit.

| issue | what was wrong | fix | evidence |
|---|---|---|---|
| #177 (external, vedantpatel41) | hole-free concave brep/faceset loops fan-triangulated from vertex 0 → notch bridged | `triangulate_simple_loop`: per-corner scale-free convexity test (sin θ > 1e-3, edges < 1e-3·max are neutral); convex → fan (bit-identical), concave → existing Newell+earcut path | synthetic L-prism 20 tris / 208 m² / 128 m³ exact vs ifcopenshell; ARK: 2 walls to exact oracle parity |
| #178 | Rust `PRODUCT_TYPES` lacked IfcGeographicElement/IfcCivilElement (+18 more classify.py take-off classes); 5 listed classes had no title-case spelling → silently SKIP; empty model with no warning | whitelist synced + `_core.product_types()` parity test; `skipped_product_type_counts` in the indexer (IfcRoot 22-char-GUID prefilter, IfcRel* rejected, ObjectType Str/Null); `m.skipped_product_types`, `summary()` key, CLI SKIPPED block, UserWarning on zero-product models | RIV gains one IfcGeographicElement at ratio 1.0000 |
| #179 | `iter_meshes()` yielded shifted f32 with no `global_shift` | `MeshIter` (re-iterable, len, `.global_shift/.frame/.unit/.stats`); frame table on every mesh entry point | far-origin fixture: streamed z + shift = `m.mesh()` within 5e-8 m |
| #180 | `StoreyRow.elevation` in file units, everything else metres | `elevation_m` (None when unit unresolved) through schemas/summary/preview/cache/diff; diff reports unit-only change | mm fixture 3000 → 3.0 |
| #183 | wasm parsed psets/quantities/materials/classifications but had no accessor | four `*Json()` accessors mirroring the DataFrames; sidecar generator + parity extended | strict parity 18/18 after regen |
| #181/#184 (mine) | wasm storeys lacked `elevation_m`; wasm summary lacked `skipped_product_types` | mirrored | parity |

Cache: `_CACHE_SCHEMA_VERSION` 31→32 (wasm mirrored), index `CACHE_VERSION` 5→6.

## Gate (all on the working-tree release wheel)

- cargo workspace tests + clippy + fmt clean (core 351 lib tests; wasm crate 11).
- pytest `tests/` with `IFCFAST_CORPUS` = 4 G55 models: 362 passed, 3 skipped, 12 min.
- `class_sweep` RIB/ARK/RIE/RIV vs `scratch/g55/baselines/*.json`: no class past ±0.005. Baselines NOT rewritten (no class moved enough to justify it; ARK IfcWall 0.9996→1.0000 is within the band).
- **ARK per-element A/B** (HEAD wheel via `git stash -u` + `class_sweep --refresh-fast` on a copy of the new cache, ios side identical): 653/12221 elements moved, all attributed:
  - 463 closed→closed: median rel 9e-8 (f32 summation order from new triangle sequence); 2 walls (`2ErBcpP9LBhOEw696w5Hxh` 0.1737→0.3017, `3POXqHuM96DwZzM2c25HD1`) to exact oracle value — the #177 fix.
  - 76 closed→open_shell and 32 open_shell→closed, **all volumes bit-identical**: `is_closed_manifold` requires edge multiplicity exactly 2; on self-touching solids (coplanar ±Z triangle pair = zero-thickness region) earcut's diagonal on a concave face can coincide with a real solid edge → multiplicity 4 with balanced winding. Triangulation-dependent label, not a geometry change. Filed **#187** with the mechanism (verified on `3POXqHuM96DwZzM2c25HA4`: 1327 edges ×2, one edge ×4).
  - 67 open→open: one IfcPlate improved 63 %→28 % error; rest tiny.
  - 15 prism_fallback: two repeated family geometries, prism bound moved < 0.9 % because it derives from surface area (which #177 changes); already ~50 % off the oracle before and after — pre-existing open-shell case.
- Review agent (opus, read-only) found 9 items; all applied before the gate: IfcRel* probe false positives (would have listed thousands of relationships as skipped products), loop-global convexity epsilon (chord + fine concave arc fanned), v32 changelog missing the #177 clause, stale faceset comment, `diff()` gating on raw elevation only, None-unsafe docstring snippet, `generate_receipts.py` raw elevation under an `_m` label, `iter_meshes` "bounded memory" wording, PEP8.
- wasm: `build.sh` (limits/parity/stream) → sidecar regen → strict parity 18/18. Site `ifcfast-site` `12fdfc3` carries regenerated Duplex sidecars (+4 new layer files) and the synced wasm.

Two brief mistakes caught by agents, recorded for honesty: my #177 brief said the L-prism area is 192 (it is 208, perimeter 40 not 32); my #178 brief's parity assertion was unsatisfiable with only two classes (20 were missing). The first test GUIDs in the indexer tests were 23 chars (the prefilter needs 22) — fixed by me.

## spruceledger
No node consulted changed what was done (no `/claims` hit for the literal strings in hand: `earcut`, `IfcGeographicElement`, `global_shift`).

## Issues touched
Commented: #177 (fix + still asking for the IFC), #178, #179, #180, #183. Closed (mine): #181, #184. Filed: #181, #182 (unit-suffix audit, 7 surfaces), #184, #185 (agent: materials rollup drops `role=direct` on both sides), #186 (skipped_product_types key casing), #187 (classifier on self-touching solids).

## Next
1. **Release**: main is 6 issues ahead of v0.5.1 with a cache-schema bump; a `v0.5.2` tag is Ed's call (bundle rule).
2. #187 classifier relaxation (even multiplicity + signed 0 = closed) — gate it with the per-class sweep + the #131 "predict oracle divergence" rule; the 108 ARK GUIDs are reproducible from `scratch/g55/cache_ab_head` vs `cache_gate178`.
3. #186 canonical entity name in core (schema-backed), then drop the case normalisation in parity.mjs.
4. #185 materials rollup (both sides, needs sidecar regen + parity).
5. #182 pick the ones worth fixing (hotswap input unit guard first).
6. Site: drop-path UI notice when `skipped_product_types` is non-empty (site tracker); site #4 Vercel integration still dead — the pushed sidecars/wasm are not live until that is fixed.
7. Scratch cleanup: `scratch/g55/cache_gate178`, `cache_ab_head` (~10 MB) can go; the older `cache_170_*`/`cache_clash170*` question from last session still open.
