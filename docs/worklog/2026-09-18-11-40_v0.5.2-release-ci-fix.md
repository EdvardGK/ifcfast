## Agent signature
- **Agent**: `claude-fable-5-1` (same session as the 2026-09-17 sweep, continued after the date change)
- **Working tree**: `/home/edkjo/workspace/inbox/ifcfast`
- **Branch**: `main` @ `e431503` → `bce4752` (release commit `f6f3010`, tag `v0.5.2`, CI fix `bce4752`)
- **Session scope**: release v0.5.2 on Ed's instruction and get CI green again
- **Touched paths**: `CHANGELOG.md`, `Cargo.toml`, `Cargo.lock`, `pyproject.toml`, `crates/core/src/source.rs`
- **Parallel sessions observed**: `none` on origin/main
- **Supersedes / superseded by**: follows `2026-09-17-15-55_tester-issue-sweep-177-184.md`

## Summary

Ed checked whether the #177 reporter had answered (no, on GitHub or email) and said: "if you believe something is fixed, err on the side of updating. Move fast." So the sweep was released as **v0.5.2** rather than waiting for a bundle: changelog section for #177/#178/#179/#180/#183 (+#181/#184, cache v32), version bump, annotated tag, tag push → CI built 4 wheels + sdist and published to PyPI (5 files, verified via the PyPI JSON). GitHub release created from the changelog section. Release note posted on the five issues.

While watching the release run I saw that **`ci` and `csg-smoke` had been red on main since 2026-09-07** — six pushes including yesterday's sweep — and nobody had looked. Neither failure was code the sweep touched, and the release workflow does not depend on them, but they were fixed in `bce4752` and both workflows are green again:

- `csg-smoke`'s `--no-default-features --features prism-csg-fast --lib polygon_bool` lane compiles the whole lib test module without `mmap`; four `source.rs` tests call `open` / `open_with` (mmap-only) and failed to compile. Gated `#[cfg(feature = "mmap")]`.
- `cargo-audit`: crossbeam-epoch 0.9.18 → 0.9.21 (RUSTSEC-2026-0204). `paste` unmaintained stays a warning.

Verified locally on the exact failing lanes plus the default lib tests before pushing.

## Lesson
I pushed `f3c2982` yesterday without checking the CI result; the failure predated me but I would not have known. Memory now says: check `gh run list` after every push. Also: the `gh run list --commit <sha>` watch produced no events for 30 min — use the plain `--limit` listing and filter on `headSha`.

## Next
1. Reporter feedback on #177 (and the four edkjo-agent issues) — none as of 2026-09-18 11:40.
2. #187 classifier relaxation, #186 canonical entity name, #185 materials rollup, #182 unit audit — unchanged priority.
3. Site #4 still blocks the pushed sidecars/wasm from going live.
